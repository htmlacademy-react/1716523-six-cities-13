

export const RentalOffersCount = {
  count : 312,
};

export enum AppRoute {
  Root = '/',
  Offers = '/offers/:id',
  Favorites = '/favorites',
  Login = '/login',
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
